// Student class will inherit Person class
// Inherit by using the command "extend"
// A class can only inherit one other class

// Student is the child class and Person is the parent class
public class Student extends Person {
    // All attributes and methods that are not private are inherited

    // Constructors are not inherited

    int student_id;

    double gpa;

    Student(){

    }
}
